Blockly.JavaScript['Angle'] = function(block) {
  var number_base_len = block.getFieldValue('Angle');
  // TODO: Assemble JavaScript into code variable.
  var code = 'ang = '+number_base_len+';';
  return code;
};
